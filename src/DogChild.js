import * as React from 'react';

const DogChild = (props) => {

    return (
        
        <img src={props.doggo} alt="Wait for the dog. WAIT FOR IT!"></img>
        
    )
};

export default DogChild;